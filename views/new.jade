extends layout

block content
    p Input The Message Here
    form(action="/new" method = "POST" )
        input(type="text" name = "messageUser" placeholder="input author here...")
        input(type="text" name = "messageText" placeholder="input message here...")
        button(type="submit") Submit